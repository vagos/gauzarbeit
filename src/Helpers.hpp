#ifndef HELPERS_HPP
#define HELPERS_HPP

#include <string>
#include <algorithm>

bool IsNumber(const std::string& s);
bool PartlyMatch(const std::string& s1, const std::string& s2, int n = 3);


#endif
